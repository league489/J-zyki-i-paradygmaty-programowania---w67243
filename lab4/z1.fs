module z1

//type user = {
//    Weight: float
//    Height: float
//}

//let calculateBMI (User: user) =
//    let heightMeters = User.Height / 100.0
//    let bmi = User.Weight / (heightMeters ** 2.0)
//    bmi

//let getBMICategory bmi =
//    match bmi with
//    | x when x < 18.5 -> "Niedowaga"
//    | x when x >= 18.5 && x <= 24.9 -> "Waga prawidłowa"
//    | x when x >= 25.0 && x <= 29.9 -> "Nadwaga"
//    | _ -> "Otyłość"

//    let zadanie1 =
//    printf "Podaj swoją wagę w kg: "
//    let weightInput = System.Console.ReadLine()
//    let weightInputFloat = float weightInput
//    printf "Podaj swój wzrost w cm: "
//    let heightInput = System.Console.ReadLine()
//    let heightInputFloat = float heightInput
//    if weightInputFloat > 0.0 && heightInputFloat > 0.0 then
//        let user = { Weight = weightInputFloat; Height = heightInputFloat }
//        let bmi = calculateBMI user
//        let category = getBMICategory bmi
//        printfn "Twoje BMI: %.2f" bmi
//        printfn "Kategoria BMI: %s" category
//    else
//        printfn "Błędne dane wejściowe."
  

