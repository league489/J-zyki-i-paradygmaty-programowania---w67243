module z5

//let NajdłuższeSłowo(text:string) = 
//    let tekst_f = List.ofArray(text.Split([|' '; '.'; ','; '\n'|], System.StringSplitOptions.RemoveEmptyEntries)) 
//    let max_długość = tekst_f |> List.map(fun x -> x.Length) |>  List.max
//    let naj_słowo = tekst_f |> List.filter(fun x -> x.Length = max_długość) 
//    printfn$"Najdłuższe słowo w tekście to: {naj_słowo} , długość : {max_długość}"

//printfn"Wprowadź tekst, w którym chcesz znaleźć najdłuższe słowo"
//let Tekst = System.Console.ReadLine()
//NajdłuższeSłowo(Tekst)