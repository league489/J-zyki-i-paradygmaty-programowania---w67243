module z4

//let Przeformatuj(text:string) =
//    let tekst_f = List.ofArray(text.Split([|';'|], System.StringSplitOptions.RemoveEmptyEntries))
//    let imie =  (tekst_f[0].[0]).ToString().ToUpper() + tekst_f[0].Substring(1)
//    let nazwisko = (tekst_f[1].[0]).ToString().ToUpper() + tekst_f[1].Substring(1)
//    let wiek = int64 tekst_f[2]
//    printfn$"{nazwisko},{imie}, ({wiek} lat)"

//let mutable TekstDoFormatu = ""

//while TekstDoFormatu <> "exit" do
//    // Pobieranie tekstu od użytkownika
//    printfn "Podaj tekst do formatu (lub wpisz 'exit' aby zakończyć):"
//    TekstDoFormatu <- System.Console.ReadLine()

//    if TekstDoFormatu <> "exit" then
//        Przeformatuj TekstDoFormatu

//printfn "Koniec programu."

    